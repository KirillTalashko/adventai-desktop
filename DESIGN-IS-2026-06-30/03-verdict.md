# 03 — Verdict

## REFINE — 23/30, ни один принцип не ноль; продуктовая основа здоровая (честность, ненавязчивость, полезность — по 3), нужно убрать дрейф системы, жаргон и dev-витрины с пользовательской поверхности.

Правило: REFINE при сумме ≥20 и отсутствии нулей — выполнено (23/30, минимум по принципу = 1).

Почему не REDESIGN: load-bearing принципы #2 (useful=3), #4 (understandable=2), #6 (honest=3) — без нулей; «провал» возникает только если затащить в скоуп инженерные демо-диалоги, а это отдельная задача декластеринга, а не пересборка продукта (anti-pattern «redesign из-за одного экрана»).

## Топ-5 ходов (наибольший рычаг)

1. **#4 + #10 — вынести/спрятать dev-демо** с пользовательской поверхности: тест echo/get-sum и пайплайн «по коду/агент» в «Инструменты MCP» (`App.kt:582–780`) и витрину сравнения в «Коннекторы» (`App.kt:498–578`) — убрать за «режим разработчика»/отдельную сборку. Это поднимает понятность и минимализм всего приложения.
2. **#4 — подписать композерные иконки**: `Extension`=MCP (`App.kt:437`), `Tune`=Коннекторы (`App.kt:457`) — добавить видимый ярлык/тултип; переименовать «Инварианты»→«Правила» (`App.kt:198`, `Dialogs.kt:241`) для конечного пользователя.
3. **#3 — консолидировать шкалы**: радиусы свести к набору брифа 10–16 (сейчас 6/8/9/10/12/16/20 — `App.kt:189,143`, `Onboarding.kt:51`); ввести spacing-шкалу 4/8/12/16/24 как единый источник.
4. **#7 + #3 — убрать эмодзи-как-иконки** (`🟢⚠️💡🤖▶`, `App.kt:556,613,619,747,750`) в пользу Material Outlined — консистентность с брифом «линейные иконки» и долговечность.
5. **#9 — тёмная тема + гейт мотива**: добавить `darkColorScheme` (сейчас `Theme.kt` light-only) и уважать reduced-motion для авто-скролла (`App.kt:311`).

## Keep (уже 3 — не трогать)
- #2 useful — пустое состояние/композер (`App.kt:341,392`).
- #5 unobtrusive — тихий хром (`App.kt:291`).
- #6 honest — предупреждения/маскирование/честные баннеры (`App.kt:261,619`, `Dialogs.kt:67`).
- #10 (продуктовый экран) — минимализм основного чата (`App.kt:291–413`).
